{
  "name": "@defi-staking/subgraph",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ defi-staking-platform"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.92.0",
    "@graphprotocol/graph-ts": "^0.36.0"
  }
}
